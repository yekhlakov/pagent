<?php

// This file produces the README.md and also serves as an example of agent integration

require ('vendor/autoload.php');


$agent = new Yekhlakov\PAgent\Agent(llm: "local");


$agent
->withTools('filesystem')
->handle(
"
**BASIC ASSUMPTIONS**
* Assume that all files are in local file system.
* Assume class and file names follow usual php notation
* The namespace `Yekhlakov\\PAgent` maps to `src/` directory

**Task steps**:
1. Explore the file `src/Agent.php`. Also explore ALL used traits (you MUST fetch their program code with your tools).
2. Produce a description of this program: its purpose, principles of operation, required configuration variables etc.
3. In a separate section describe the tools available to the Agent. Include the list of tools as a table containing tool name and description.
   Extract tool names and descriptions from the program code. (Note: tool methods are annotated with `LlmTool` attribute)
5. Mention the web interface of the agent and how to start it (point the web server to `/public` directory of this repo as usual)
5. Mention in the end of the description that it was generated by you (an agent).
6. WRITE the description you produced to local file `README.md` (overwriting its old contents) and finish work.

**IMPORTANT**: The README you produce is intended to be posted into public repository. Formulate appropriately. This is a description of a program, not a report.

");

